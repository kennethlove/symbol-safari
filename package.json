{
  "name": "symbol-finder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/dev.mjs 3000",
    "deploy": "wrangler deploy",
    "pages:dev": "wrangler pages dev public --port 3000 --do=ROOMS=GameRoom"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
